describe("object directive", function() {

	// it should create a fieldset
	// it should create a form with one input field
	// input field content set to X
	// it should result.test == X
	//$scope.theschema = { type: 'object', kids:
	//	{ type: 'string', name: 'test' }
	//};

	// it should throw exception: no name a kid in object -{object}-
	//$scope.theschema = { type: 'object', kids:
	//	{ type: 'string' }
	//};

});
